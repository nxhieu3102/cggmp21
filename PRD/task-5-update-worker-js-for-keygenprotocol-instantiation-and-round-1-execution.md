# Task 5: Update `worker.js` for `KeygenProtocol` Instantiation and Round 1 Execution

**ID:** 5
**Status:** pending
**Priority:** high
**Dependencies:** [4]

## Description
Modify `cggmp21-wasm/web/worker.js` to utilize the new stateful WASM `KeygenProtocol`. This involves instantiating the protocol object and executing its round 1 method, including handling message passing to other parties.

## Implementation Details
In `cggmp21-wasm/web/worker.js`:
1. Replace the existing stateless round 1 key generation logic.
2. Instantiate the new WASM `KeygenProtocol` object (e.g., `const keygenProtocol = new KeygenProtocolWasm(params...);`). Ensure all necessary initialization parameters (like party index, threshold, total parties, session ID) are correctly passed from JavaScript to the WASM constructor.
3. Call the exposed WASM method for `round1_generate_commitment()` on the instantiated object.
4. Implement logic to send the resulting round 1 message to other participants via the existing communication channel (e.g., WebSocket server).

## Test Strategy
Perform a manual integration test within a browser environment. Initiate the key generation process. Verify that the first round's message is correctly generated by the new stateful WASM module, is properly formatted, and is successfully broadcast to other (mock or real) parties. Monitor the browser's developer console for any JavaScript or WASM-related errors. 
