# Task 7: Expose and Integrate `round1_receive_commitments_and_prep_reliability_check` (WASM & JS)

**ID:** 7
**Status:** pending
**Priority:** high
**Dependencies:** [6]

## Description
Expose the `round1_receive_commitments_and_prep_reliability_check` Rust method to WASM via `wasm-bindgen`. Update `worker.js` to call this new WASM method after collecting all round 1 messages, and handle the potential `MsgReliabilityCheck` message.

## Implementation Details
1. In `cggmp21-wasm`: Add a `#[wasm_bindgen]` method to the wrapper struct that calls the Rust `KeygenProtocol::round1_receive_commitments_and_prep_reliability_check` method. Handle `JsValue` (de)serialization for `RoundMsgs<MsgRound1<D>>` input and `Option<MsgReliabilityCheck<D>>` output.
2. In `cggmp21-wasm/web/worker.js`: After successfully collecting all round 1 commitment messages from other parties, call this new WASM method. If a `MsgReliabilityCheck` message is returned (i.e., not null/undefined), broadcast it to other participants.

## Test Strategy
Integration test: Run the protocol in a browser setup. After round 1 messages are exchanged, verify that `worker.js` calls the new WASM method. If `reliable_broadcast_enforced` is set, ensure `MsgReliabilityCheck` messages are correctly generated by WASM, returned to JS, and then broadcast. Monitor for errors. 
