# Task 13: Expose and Integrate `finalize_keygen` and Return `CoreKeyShare` (WASM & JS)

**ID:** 13
**Status:** pending
**Priority:** high
**Dependencies:** [12]

## Description
Expose the `finalize_keygen` Rust method to WASM. Update `worker.js` to call this final WASM method after collecting all Round 3 messages, and then handle/store the returned `CoreKeyShare`.

## Implementation Details
1. In `cggmp21-wasm`: Add a `#[wasm_bindgen]` method for `KeygenProtocol::finalize_keygen`. Ensure the `CoreKeyShare<E>` Rust struct can be correctly serialized to a `JsValue` (using `serde-wasm-bindgen`).
2. In `cggmp21-wasm/web/worker.js`: After collecting all `MsgRound3` messages, call this final WASM method. Receive the `CoreKeyShare` object in JavaScript. Implement logic to store or use this key share as required by the application (e.g., make it available to the main browser thread or store it securely if applicable).

## Test Strategy
End-to-end test: Successfully complete a full threshold key generation process in the browser with multiple (mock or real) parties. Verify that a valid `CoreKeyShare` is generated by the WASM module and correctly returned to the JavaScript environment in `worker.js`. Inspect the structure and content of the JS `CoreKeyShare` object to ensure it matches expectations and contains all necessary information (public key, secret share, etc.). 
 